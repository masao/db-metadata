[簡単な設置手順]

% cvs -d ~masao/.cvsroot co db-metadata
% cd db-metadata
% make

% ln -s ~yuka/public_html/db-template/db-metadata/data .

% cd member
% ln -s ../data .
% ln -s ../browse.cgi .
% ln -s ../index.cgi .
